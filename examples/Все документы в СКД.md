##### Требует переосмысления

```
Результат.Очистить();

МассивМетаданных = Метаданные.Документы;	
СписокДокументов = Новый ТаблицаЗначений;
СписокДокументов.Колонки.Добавить("Дата",,"Дата");
СписокДокументов.Колонки.Добавить("ВидДокумента",,"ВидДокумента");
СписокДокументов.Колонки.Добавить("Номер",,"Номер");
СписокДокументов.Колонки.Добавить("Ответственный",,"Ответственный");

Для Каждого Мета Из МассивМетаданных Цикл

	Попытка
		// Создаю запрос на основе Мета.Имя и Объединить все
		// ...
	Исключение
		Сообщить("Проблема в " + Мета.Имя + Символы.ПС + ОписаниеОшибки());	
	КонецПопытки;
КонецЦикла;
// Выполняю запрос заполняя СписокДокументов
// ...
	 
НаборыДанных = Новый Структура("СписокДокументов", СписокДокументов);

СКД = РеквизитФормыВЗначение("Объект").ПолучитьМакет("ОсновнаяСхемаКомпоновкиДанных");

КомпМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
МакетКомп = КомпМакета.Выполнить(СКД, СКД.НастройкиПоУмолчанию);
ПроцессорКомпДанных = новый ПроцессорКомпоновкиДанных;
ПроцессорКомпДанных.Инициализировать(МакетКомп, НаборыДанных);

Вывод = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
Вывод.УстановитьДокумент(Результат);
Вывод.Вывести(ПроцессорКомпДанных, истина);
```